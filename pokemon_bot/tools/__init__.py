"""Tool registry — wraps service functions as named callable tools.

Ported from open-react-template/ed_tools.ts. The TS `wrapTool` adds
ElasticDash telemetry; here it is a passthrough so the CLI can run without
any observability backend.
"""
